export function AiBadge() {
  return (
    <span
      className="absolute bottom-2 left-2 z-10 bg-ink/60 px-1.5 py-0.5 text-[0.6rem] font-semibold uppercase tracking-[0.1em] text-white/90 backdrop-blur-sm"
      title="KI-generiertes Bild"
    >
      AI
    </span>
  );
}
